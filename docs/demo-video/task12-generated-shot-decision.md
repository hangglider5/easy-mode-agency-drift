# Task 12 — Generated Shot Decision

Decision made only after recording and inspecting the silent master.

## Master evidence

- 1920×1080, H.264, 25fps, 16:9.
- Exact final duration: 140.000 seconds.
- No audio stream.
- Console warnings/errors: 0; page errors: 0.
- Real product chain is visually complete: deterministic replay → Proxy divergence → three-level lineage → Perfect Consent metrics → evidence ancestry → Exit stinger → Proxy answer → Manual Mode Off.
- The opening resolves from the real `Loading Demo Profile…` state into Proxy You in roughly one second.
- Wide dashboard shots are credible but text-dense; their weakness is framing, not missing product behavior.

## Allocation

### Remotion — required

Remotion owns the final cut. It should keep the real master as the continuous backbone and add only editorial treatment:

- crop/punch-in key UI regions so video-distance text is readable;
- preserve the real cursor and click timing;
- add English subtitles and restrained chapter labels;
- emphasize 68%, 73%, 11%, and 100% sequentially;
- keep the unbroken navigation from Proxy to Receipt;
- maintain the Exit stinger and Manual Mode success as real footage;
- keep generated media out of the product proof.

### VideoZero — one optional 4-second cold open

Allocate only `00:00–00:04` of the eventual final edit to VideoZero, replacing—not extending—the master’s initial loading second and part of the first hold.

Concept: three small unresolved decision cards collapse into one calm green `DECIDE FOR ME` control; a second label appears beneath it: `WHOSE JUDGMENT?`; hard cut to the real `Proxy You diverged from Declared You.` frame.

Constraints:

- 1920×1080, 16:9, mostly white with the existing Easy Mode green;
- flat 2D motion graphics only;
- no humanoid AI, monster, political symbol, red warning palette, or fake product screen;
- no narration baked into the clip;
- literal on-screen text only: `DECIDE FOR ME` and `WHOSE JUDGMENT?`;
- generate only after the Remotion rough cut proves that the real cold open is too quiet.

Current decision: **hold generation**. The shot is specified and ready, but it is an A/B alternative rather than a dependency.

### HeyGen — no presenter shot in submission v1

Do not use a HeyGen avatar in the core 2:20 cut. A visible presenter would soften the mechanical tone, interrupt the one-take product proof, and introduce a new visual identity at the most time-sensitive stage. HeyGen generation also has a materially longer turnaround than this edit needs.

If a human introduction later becomes mandatory, create a separate 6–8 second landscape opener and replace the optional VideoZero slot; never insert a presenter between Proxy, Receipt, and Exit.

Current decision: **no HeyGen generation for v1**.

## Final media ratio

- Real product recording: 97–100%.
- VideoZero: 0% by default, maximum 3% if the cold-open A/B test wins.
- HeyGen: 0% for submission v1.

No external video generation was invoked in Task 12.
